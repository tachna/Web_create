<!DOCTYPE html>
<html lang="ko">

<head>

	<style>
		#header a{
			color: white;
		}
	</style>
</head>

<body>
	<div id="wrap">


        <div class="bodytext_area box_inner">
           
            <table
              class="bbsListTbl"
              summary="번호, 제목, 조회수, 작성일 등을 제공하는 표"
            >
              <caption class="hdd">
                [DB에서 데이터 불러오는 페이지]
              </caption>
              <thead>
                <tr>
                  <th scope="col">번호</th>
                  <th scope="col">내용</th>
                  <th scope="col">사용자</th>
                  <th scope="col">사용일</th>
                </tr>
              </thead>
              <tbody>
                <% for (var i = 0; i < posts.length; i++){ %>
                <tr>
                  <td><%= posts[i]._id %></td>
                  <td class="tit_notice">
                    <a href="/detail/<%= posts[i]._id %>"
                      ><%= posts[i].제목 %></a
                    >
                  </td>
                  <td><%= posts[i].id %></td>
                  <td><%= posts[i].작성일 %></td>
                </tr>
                <% }%>
              </tbody>
            </table>
        </div>
	</div>


<script>
	$('#search').click(function(){
		var 입력한값 = $('#search-input').val();
		window.location.replace('/search?value='+ 입력한값) // 버튼 누르면 /search로 이동함 (get요청함)
	});
</script>
<script src="/public/js/myscript.js"></script>
</body>

</html>